# 《明日方舟》术语库 — 韩文（`ko-KR`）

[← 返回游戏总说明](../README.md)

本目录是**以 `ko-KR`（한국어，韩文）为目标语言**的术语库：每一条都是「其他语言词条 → 韩文」的对照。
共 **13,950** 条词条（20 个分类词条数之和，跨分类去重后为 12,861 条）、**53,228** 行对照；
`tgt_lng` 列固定为 `ko-KR`，`source` 列收录同一词条在其余 4 种语言（`zh-CN` / `zh-TW` / `en-US` / `ja-JP`）中的写法。

## 文件

- `NN_<分类>.csv` — 共 20 个，即 `01_干员名称.csv` … `20_游戏机制.csv`。
  `source,target,tgt_lng` 三列，可直接导入沉浸式翻译等 CAT / 术语管理工具。
- `_all.json` — 上述 20 个 CSV 的合并版（UTF-8，无 BOM 的 JSON），
  结构为 `{"tgt_lng": "ko-KR", "categories": {"<分类>": [{"source","target","tgt_lng"}, …]}}`，
  共收录 53,228 条对照，内容与 20 个 CSV 完全一致。
- 本说明文件 `README_zh-CN.md`，以及韩文版 `README.md`。

> 分类文件名在所有语言目录中保持中文（如 `01_干员名称.csv`），便于跨语言对照与脚本统一处理。

## 分类与条数

| 分类 | 主题（韩文名称） | 词条数 | 对照行数 |
| --- | --- | ---: | ---: |
| `01_干员名称` | 干员名称 / 오퍼레이터명 | 416 | 1,451 |
| `02_干员异格` | 干员异格 / 이격 오퍼레이터 | 34 | 130 |
| `03_职业与分支` | 职业与分支 / 직업과 분기 | 81 | 284 |
| `04_技能名称` | 技能名称 / 스킬명 | 1,307 | 4,912 |
| `05_技能描述关键术语` | 技能描述关键术语 / 스킬 설명 핵심 용어 | 291 | 1,138 |
| `06_天赋` | 天赋 / 특성 | 566 | 2,114 |
| `07_潜能` | 潜能 / 잠재능력 | 115 | 439 |
| `08_模组` | 模组 / 모듈 | 844 | 3,303 |
| `09_敌人` | 敌人 / 적 | 1,351 | 5,088 |
| `10_BOSS` | BOSS | 203 | 709 |
| `11_关卡` | 关卡 / 스테이지 | 2,103 | 7,826 |
| `12_地区` | 地区 / 지역 | 192 | 707 |
| `13_阵营` | 阵营 / 진영 | 44 | 167 |
| `14_活动` | 活动 / 이벤트 | 212 | 835 |
| `15_道具` | 道具 / 아이템 | 517 | 2,007 |
| `16_装备` | 装备 / 장비（전술 장비·생식연산·비밀 기록） | 1,289 | 4,949 |
| `17_材料` | 材料 / 재료 | 674 | 2,494 |
| `18_剧情专有名词` | 剧情专有名词 / 스토리 고유명사 | 1,334 | 4,962 |
| `19_UI与系统术语` | UI 与系统术语 / UI·시스템 용어 | 2,135 | 8,777 |
| `20_游戏机制` | 游戏机制 / 게임 시스템 | 242 | 936 |
| **合计** | | **13,950** | **53,228** |

- **词条数** = 该分类 CSV 中 `target` 列的**去重条数**（同一韩文译名只算一条）；
- **对照行数** = CSV 的**数据行数**（不含表头）。同一条词条会因其他 4 种语言的 source 展开为多行，
  且 `source` 与 `target` 写法完全相同的行不写入，因此行数略少于「词条数 × 4」。
- 「合计」为 20 个分类的直接相加；同一韩文译名会同时出现在多个分类中，把 20 个分类合并后跨分类
  去重，得 **12,861** 条唯一译名。

## 说明

- **译文来源与对齐方式**：全部来自各区服官方客户端解包数据表（上游 `ArknightsGamedata` 仓库的
  `kr` 区，即韩服），所有表格以条目 ID（如 `char_002_amiya`、`skchr_amiya_2`）为键跨 5 个区服对齐，
  因此是**官方本地化文本的逐条对照，非二次翻译**。
- **语言标签**：`ko-KR` = 한국어（韩服）。本目录 `target` 列固定为韩文官方译名；
  `source` 列可能来自 `zh-CN` / `zh-TW` / `en-US` / `ja-JP` 任意一种。
  只想保留「日→韩」单向词表时，只保留 `source` 含日文假名的行即可。
- **编码**：UTF-8 with BOM + CRLF，Excel 双击可直接打开且谚文不乱码；
  `_all.json` 为 UTF-8（无 BOM）。
- **已知限制**：
  - `05_技能描述关键术语` 与 `20_游戏机制` 由文本自动挖掘（占位符分段 + 全语料投票），
    少数条目仍是短语片段而非严格术语，建议按需人工复核；
  - 关卡、活动等大量同名词条会各自成行，未做跨条目合并；
  - 干员异格仅收录异格形态本身，基础干员见 `01_干员名称`；
  - 各区服进度不同，尚未实装的内容不会出现在本目录中（生成时要求同一 ID 至少有两种语言存在），
    因此 `ko-KR` 部分分类的行数少于 `zh-CN`。

## 免责声明

本目录为个人整理与维护的**非官方**术语资料，仅用于个人学习、研究及辅助 AI 翻译软件的术语匹配。
本库与《明日方舟》及其开发商、发行商、代理商、运营商、版权方不存在任何从属、授权、合作或代理关系；
库中译名不代表官方立场，不保证准确、完整或与游戏当前版本一致，**不应被视为官方术语表或官方本地化文件**。
游戏名称、角色名称、专有名词、商标等知识产权均归各自权利人所有。
完整条款见仓库根目录 `README.md` / `README_EN.md` / `README_JP.md`。
